"use server"

export default async function main() {
	// await seedCategories()
	// await seedCourses()
	// await seedAttachments()
	// await seedChapters()
	// await seedMuxData()
	// await updateMuxDate()
	// await openFirstVideo()
}
